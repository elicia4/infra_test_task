---
- name: Reload systemd daemon
  ansible.builtin.systemd_service:
    daemon_reload: true
  listen: "Restart ssh"

- name: Restart ssh
  ansible.builtin.systemd_service:
    name: ssh
    state: restarted
  listen: "Restart ssh"
